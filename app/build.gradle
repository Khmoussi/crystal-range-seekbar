apply plugin: 'com.android.application'

android {
    compileSdkVersion 27
    buildToolsVersion "27.0.3"

    defaultConfig {
        applicationId "com.crystal.crystalrangeseekbar"
        minSdkVersion 17
        targetSdkVersion 27
        versionCode 1
        versionName "1.0"
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
    }
}

dependencies {
    implementation fileTree(dir: 'libs', include: ['*.jar']) // Replace 'compile' with 'implementation'
    testImplementation 'junit:junit:4.12' // Replace 'testCompile' with 'testImplementation'
    implementation project(':crystalrangeseekbar') // Replace 'compile' with 'implementation'
    implementation 'com.android.support:appcompat-v7:27.0.2' // Replace 'compile' with 'implementation'
    implementation 'com.android.support:design:27.0.2' // Replace 'compile' with 'implementation'
}
